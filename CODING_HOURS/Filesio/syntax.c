#include <stdio.h>

int main(){
    // FILE *fptr = fopen("const char* name","const char* mode");  //syntax
    //fptr = ("file.txt","r")  //syntax
    //r,w,a,w+,r+,wb,rb,ab etc
    // relative path = D:/CTSD_KLH/CODING_HOURS/filei/o
    //absolute path = "File.txt"
    //fopen = success = fptr
    //fopen = failure = null
    //fopen,fprintf,fscanf used only for program doing in files
    //scanf,printf used for screen/terminal,monitor
    //fgets,fputs used for fscanf,fprintf,respectively
    // fscanf(fptr , formatspecifier , buffer);  //syntax
    //fclose mandatory

    return 0;
}